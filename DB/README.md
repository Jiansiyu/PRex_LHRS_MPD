# PREX GEM detector mapping instructions








### trouble solve

#### 1) int Decoder::THaSlotData::getData(int, int) const: Assertion `chan >= 0 && chan < (int)maxc && hit >= 0 && hit < numHits[chan]' failed.
  